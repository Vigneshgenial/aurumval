{
  "build": {
    "builder": "NIXPACKS",
    "detectPackageManager": false
  },
  "deploy": {
    "startCommand": "npm install && npm run build && node dist/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
